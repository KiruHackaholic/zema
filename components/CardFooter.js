import React from 'react';
import { View } from 'react-native';

const CardFooter = (props) => {

    return (
        <View>
            {props.children}
        </View>
    );

};

const styles = {

};

export default CardFooter;
